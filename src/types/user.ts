type RegisterUser = {
  name: string;
  email: string;
  password: string;
};

type RegisterUserPlatforms = {
  name: string;
  email: string;
  image: string;
};

type LoginUser = {
  email: string;
  password: string;
};
